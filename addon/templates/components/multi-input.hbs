<div class='inputs-group'>{{#each inputs as |input index|}}
  {{#if renderComponent}}
    {{component renderComponent input.value index}}
  {{else}}
    <div class='input-value'>
      {{#if canDeleteInput}}
        <span class='delete-input' {{action 'deleteInput' input.id}}>×</span>
      {{/if}}
      {{input.value}}
    </div>
  {{/if}}
{{/each}}
</div>
<div class='input-wrapper'>
   <input type={{type}} autocomplete="off"
      autocorrect="off" autocapitalize="off"
      spellcheck="false" role="combobox"
      class="multi-input-input"
      value={{currentInput}}
      placeholder={{inputPlaceholder}}
      oninput={{action 'onInput'}}
      onblur={{action 'onBlur'}}
      onkeydown={{action 'onKeydown'}} >
</div>
{{#if _error.full_message}}
  <div class='error-message'>
    {{_error.full_message}}
  </div>
{{/if}}